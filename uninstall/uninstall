#!/bin/bash

echo "Removing APC UPS Daemon"
# -y ist noetig: ohne die Bestaetigung wartet apt-get auf eine Eingabe, die
# im Deinstallationslauf niemand geben kann.
sudo apt-get remove -y apcupsd
exit 0
